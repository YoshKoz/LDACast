# build outputs
target/
ui/LDACast/bin/
ui/LDACast/obj/

# runtime artifacts (personal link keys, packet logs, local logs)
*.pklg
*_link_keys.tlv
btstack_*.tlv
*.log

# IDE / OS
.vs/
.vscode/
Desktop.ini
Thumbs.db

# Windows reserved device name captured by a stray `> NUL` redirect under bash;
# git can never index it ("short read while indexing NUL") but it breaks add -A.
NUL

<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="LDACast.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="12,8,12,8" RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Settings" />

        <StackPanel Grid.Row="1" Spacing="8">
            <TextBlock FontWeight="SemiBold" Text="Bluetooth radio" />
            <StackPanel Orientation="Horizontal" Spacing="8">
                <TextBlock VerticalAlignment="Center" Text="Mode:" />
                <TextBlock x:Name="ModeText" VerticalAlignment="Center" FontWeight="SemiBold" Text="…" />
                <Button Click="ToWindows_Click" Content="To Windows" />
                <Button Click="ToLdac_Click" Content="To LDAC" />
                <Button Click="Refresh_Click" Content="Refresh" />
                <ProgressRing x:Name="BusyRing" IsActive="False" Width="20" Height="20" />
            </StackPanel>
            <TextBlock
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                TextWrapping="Wrap"
                Text="One radio serves one stack at a time. Streaming needs LDAC mode; normal Bluetooth needs Windows mode. Switching elevates itself (UAC) and takes a few seconds." />
        </StackPanel>

        <StackPanel Grid.Row="2" Spacing="8">
            <TextBlock FontWeight="SemiBold" Text="Engine" />
            <TextBlock x:Name="PathsText" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />
        </StackPanel>

        <ScrollViewer Grid.Row="3">
            <TextBlock
                x:Name="DetailText"
                FontFamily="Consolas"
                FontSize="12"
                TextWrapping="Wrap" />
        </ScrollViewer>
    </Grid>
</Page>
